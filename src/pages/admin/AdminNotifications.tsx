import AdminPlaceholderPage from "./AdminPlaceholderPage";

const AdminNotifications = () => (
  <AdminPlaceholderPage
    title="Notifications"
    description="Send notifications to all users and keep a log."
  />
);

export default AdminNotifications;
